- name: Install X11 desktop, streaming display :10, and RDP to same display
  hosts: vps
  become: true
  tasks:
    - name: Install desktop and display packages
      ansible.builtin.apt:
        name:
          - chromium
          - dbus-x11
          - fonts-ipafont-gothic
          - fonts-ipafont-mincho
          - fonts-noto-cjk
          - pavucontrol
          - pipewire
          - pipewire-audio
          - pipewire-pulse
          - wireplumber
          - x11-xserver-utils
          - x11vnc
          - xfce4
          - xfce4-goodies
          - xorg
          - xrdp
          - xvfb
        state: present
        update_cache: true

    - name: Install makamujo Xvfb unit (:10 2560x1440)
      ansible.builtin.copy:
        dest: /etc/systemd/system/makamujo-xvfb.service
        mode: "0644"
        content: |
          [Unit]
          Description=Makamujo streaming display :10 (2560x1440)
          After=network.target

          [Service]
          Type=simple
          ExecStart=/usr/bin/Xvfb :10 -screen 0 2560x1440x24 -ac +extension GLX +render -noreset
          Restart=on-failure
          RestartSec=2

          [Install]
          WantedBy=multi-user.target

    - name: Install makamujo x11vnc unit (share :10 on localhost:5900)
      ansible.builtin.copy:
        dest: /etc/systemd/system/makamujo-x11vnc.service
        mode: "0644"
        content: |
          [Unit]
          Description=Makamujo x11vnc on :10
          After=makamujo-xvfb.service
          Requires=makamujo-xvfb.service

          [Service]
          Type=simple
          ExecStart=/usr/bin/x11vnc -display :10 -forever -shared -rfbport 5900 -localhost -nopw -xkb
          Restart=on-failure
          RestartSec=2

          [Install]
          WantedBy=multi-user.target

    - name: Enable and start makamujo-xvfb
      ansible.builtin.systemd:
        name: makamujo-xvfb
        enabled: true
        state: started
        daemon_reload: true

    - name: Enable and start makamujo-x11vnc
      ansible.builtin.systemd:
        name: makamujo-x11vnc
        enabled: true
        state: started
        daemon_reload: true

    - name: Ensure xrdp VNC session for Makamujo display exists
      ansible.builtin.blockinfile:
        path: /etc/xrdp/xrdp.ini
        marker: "; {mark} ANSIBLE MANAGED MAKAMUJO VNC SESSION"
        insertafter: EOF
        block: |
          [Makamujo]
          name=Makamujo
          lib=libvnc.so
          ip=127.0.0.1
          port=5900
          username=ask
          password=ask

    - name: Prefer XFCE when a local X session is used
      ansible.builtin.copy:
        dest: /etc/xrdp/startwm.sh
        mode: "0755"
        content: |
          #!/bin/sh
          if test -r /etc/profile; then
            . /etc/profile
          fi
          export XDG_SESSION_TYPE=x11
          export XDG_CURRENT_DESKTOP=XFCE
          unset DBUS_SESSION_BUS_ADDRESS
          exec dbus-launch --exit-with-session startxfce4

    - name: Enable and start xrdp
      ansible.builtin.service:
        name: xrdp
        state: restarted
        enabled: true

    - name: Enable and start xrdp-sesman
      ansible.builtin.service:
        name: xrdp-sesman
        state: restarted
        enabled: true
