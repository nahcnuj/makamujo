[defaults]
inventory = inventory/hosts.yml
roles_path = roles
# Uncomment after creating a local password file (never commit it):
# vault_password_file = .vault_pass
host_key_checking = True
interpreter_python = auto_silent

[privilege_escalation]
become = True
